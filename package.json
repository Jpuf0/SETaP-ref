{
  "name": "project",
  "version": "1.0.0",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "postinstall": "bun --filter=backend db:push",
    "dev": "bun --workspaces dev",
    "dev:frontend": "bun --filter=frontend dev",
    "dev:backend": "bun --filter=backend dev"
  },
  "dependencies": {
    "@biomejs/biome": "^2.5.5"
  }
}
